<%= form_with(url: test_conversation_path, local: true, method: "post") do |f| %>
  <%= f.hidden_field :user_id, value: user_id %>
  <%= f.submit "Message", class: "font-valera field-button" %>
<% end %>