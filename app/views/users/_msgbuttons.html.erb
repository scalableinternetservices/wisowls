<%= form_with(url: createmsg_conversation_path, local: true, method: "post") do |f| %>
  <%= f.hidden_field :user_id, value: user_id %>
  <div class="field-button-fixed">
    <%= f.submit "Message", class: "font-valera", style: "width: 100%; height: 100%; background: transparent; border: none;" %>
  </div>
<% end %>